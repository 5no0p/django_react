build:
  docker:
    web: Dockerfile
run:
  web: gunicorn mainapp.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: web
  command:
    - npm run build
    - python manage.py collectstatic --noinput
